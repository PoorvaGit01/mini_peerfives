<h2>Users</h2>

<table>
  <tr>
    <th>ID</th>
    <th>Name</th>
    <th></th>
  </tr>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.id %></td>
      <td><%= user.name %></td>
      <td><%= link_to "New User", new_user_path, class: "user-button" %></td>
    </tr>
  <% end %>
</table>
